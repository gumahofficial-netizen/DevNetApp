package com.gumah.devnet.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Color Palette
val SlateBg = Color(0xFF0F172A)       // Sleek dark background
val CardBg = Color(0xFF1E293B)        // Slate dark surfaces
val PrimaryCyan = Color(0xFF38BDF8)   // Electric terminal light-blue / cyan
val SecondarySlate = Color(0xFF94A3B8) // Muted slate text
val LightText = Color(0xFFF1F5F9)     // Off-white readable text
val AccentTeal = Color(0xFF2DD4BF)    // Neon teal for success/highlights
val DarkRose = Color(0xFFFB7185)      // Rose pink for likes/errors
val BorderSlate = Color(0xFF334155)   // Border outlines
val CodeBg = Color(0xFF020617)        // Pure pitch dark for code blocks
val OnPrimaryDark = Color(0xFF0F172A) // High contrast dark text on primary

// Light Theme Fallback (Clean minimal white/slate)
val LightBg = Color(0xFFF8FAFC)
val LightCard = Color(0xFFFFFFFF)
val LightPrimary = Color(0xFF0284C7)
val LightTextDark = Color(0xFF0F172A)

